<?xml version="1.0" encoding="UTF-8"?>

<module>

    <!-- Individual setting parameters -->
    <ind_settings>
        <parameter name="femur_length" label="Femur Length (cm)" type="double" default="12"/>
        <parameter name="tibia_length" label="Tibia Length (cm)" type="double" default="12"/>
        <parameter name="shoulder_offset" label="Shoulder Offset (cm)" type="double" default="4"/>
    </ind_settings>

    <!-- Configuration-level settings parameters -->
    <config_settings>

    </config_settings>

</module>